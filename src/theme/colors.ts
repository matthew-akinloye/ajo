export const colors = {
  // Brand – Primary Green (from palette: #144D24)
  primary: "#144D24",
  primaryLight: "#3D9556",          // lighter green (palette: #3D9556)
  primaryDark: "#01230A",           // very dark green (palette: #01230A)
  primarySoft: "#A6E8B8",           // soft green (palette: #A6E8B8)
  primaryPressed: "#000501",        // darkest (palette: #000501)
  primaryTint: "#E1FDE9",           // light tint (palette: #E1FDE9)

  // Backgrounds – mostly whites and light grays from palette
  background: "#FFFFFF",
  surface: "#F5F5F5",               // palette: #F5F5F5
  surfaceSecondary: "#EDEDED",      // palette: #EDEDED
  surfaceTertiary: "#E3E3E3",       // palette: #E3E3E3
  surfaceGray: "#F2F2F2",           // palette: #F2F2F2
  surfaceGray2: "#E7E7E7",          // palette: #E7E7E7

  // Text – from palette grays and dark
  textPrimary: "#292D32",           // dark gray (palette: #292D32)
  textSecondary: "#81848F",         // mid gray (palette: #81848F)
  textTertiary: "#999999",          // light gray (palette: #999999)
  textInverted: "#FFFFFF",          // white

  // Borders & Dividers – from palette grays
  border: "#CFCFCF",                // palette: #CFCFCF
  divider: "#E6E6E6",               // palette: #E6E6E6

  // Status
  success: "#22863E",               // green (palette: #22863E)
  successTint: "#D7FEE2",           // light green (palette: #D7FEE2)
  warning: "#F59E0B",               // kept as is (not in palette, can be replaced later)
  error: "#FD4E61",                 // red (palette: Foundation/Plana Red/Normal)
  errorTint: "#FEE2E2",             // kept as light red (not in palette, but matches well)
  info: "#6750A4",                  // purple (palette: #6750A4) – used as info

  // Semantic
  canvas: "#F5F5F5",                // same as surface
  overlay: "rgba(41,45,50,0.55)",   // from palette: #292D32 with opacity (approx 55%)
  skeleton: "#EDEDED",              // palette: #EDEDED

  // Accent Colors (derived from palette)
  accentPurple: "#6750A4",          // purple
  accentIndigo: "#7C7A85",          // grayish indigo (not exact, but close)
  accentBlue: "#292D32",            // dark gray (used as blue alternative)

  // Neutrals
  white: "#FFFFFF",
  black: "#020617",                 // kept as is (not in palette, but fine)
};